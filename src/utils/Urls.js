const BASE_URL = 'http://localhost:5173'

const Urls = {
    index: `${BASE_URL}/`,
    contacts: `${BASE_URL}/contacts/`,
    blog: `${BASE_URL}/blog/`,
    blogDetail: `${BASE_URL}/blog/:id/`,
}

export default Urls;